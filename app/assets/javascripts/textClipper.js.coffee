class @TextClipper
  setup: ->
    # clippable = $('.clippable')
    # clippable.each ->
    #   $this = $(this)
    #   truncateLength = 60
    #   if $this.html().length > truncateLength
    #     content = '<div class="truncated">' + jQuery.trim($this.html()).substring(0, truncateLength).trim(this) + '&hellip;</div>'
    #   else 
    #     content = '<div class="truncated">' + $this.html() + '</div>'
    #   $this.wrapInner('<div class="full" />').append(content)

    # clippable.click ->
    #   $this = $(this)
    #   $this.toggleClass("unclipped")